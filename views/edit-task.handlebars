<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <main class="form-container">
        <h1>Edit task</h1>
        <form class="task-form">
            <label for="title">Title: </label>
            <input type="text" name="title" id="title" value="{{task.title}}" required>

            <label for="description">Description: </label>
            <textarea name="description" id="desription" rows="4" required>{{task.description}}</textarea>
        
            <label for="priority">Priority: </label>
            <select name="priority" id="priority">
                <option value="High" class="priority High"{{#if (eq task.priority "High")}}selected{{/if}}>High</option>
                <option value="Moderate" class="priority Moderate" {{#if (eq task.priority "Moderate")}}selected{{/if}}>Moderate</option>
                <option value="Low" class="priority Low" {{#if (eq task.priority "Low")}}selected{{/if}}>Moderate</option>
            </select>
            <select name="status" id="status">
                <option value="Pending" class="status Pending"{{#if (eq task.status "Pending")}}selected{{/if}}>Pending</option>
                <option value="Completed" class="status Completed" {{#if (eq task.status "Completed")}}selected{{/if}}>Completed</option>
                <option value="Cancelled" class="status Cancelled" {{#if (eq task.status "Cancelled")}}selected{{/if}}>Cancelled</option>
            </select>
            <label for="dueDate">Due Date: </label>
            <input type="date" name="dueDate" id="dueDate" value="{{task.dueDate}}" required>

            <button type="submit" class="edit-task">Save Changes</button>
        </form>
        <a href="/myTasks" class="back-btn">Back to My Tasks</a>
    </main>
    <script src="tasks.js"></script>
</body>
</html>